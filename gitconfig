[color]
	ui = auto
[user]
	name = Chris Skalenda
	email = chris@skalenda.org
[alias]
	co = checkout
[merge]
	tool = meld
[diff]
	guitool = meld
[core]
	editor = vim
	autocrlf = false
	excludesfile = ~/.git_global_ignore
[push]
	default = current
[color "status"]
  added = green bold
  changed = red bold strike
  untracked = cyan
  branch = yellow black bold ul
[pull]
	ff = only
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[init]
	defaultBranch = main
